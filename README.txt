Takes an upright(avoid tilting too much), frontal face image of dog as input
save the input images in images folder
run detector.py to crops image down to just nose
then gives a prediction if dog has stenotic nares

See details in training_models folder